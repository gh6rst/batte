apiVersion: v1
kind: Service
metadata:
  name: battufly
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "battufly"
    port: 8080
  selector:
    app: battufly
